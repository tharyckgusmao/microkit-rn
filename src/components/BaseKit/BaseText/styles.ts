import {StyleSheet} from 'react-native';
import theme from '../../../theme';

export const styles = StyleSheet.create({
  default: {
    fontSize: theme.size.primary,
    fontFamily: theme.font.regular,
  },
});
