import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  ctn: {
    flex: 1,
    flexDirection: 'column',
    position: 'absolute',
    left: 0,
    top: 0,
    right: 0,
    width: '100%',
    height: '100%',
    opacity: 1,
    bottom: 0,
  },
});
