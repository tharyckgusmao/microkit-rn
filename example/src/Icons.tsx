import type { FC } from 'react';
import React from 'react';
import { Box, Icon } from 'react-native-microkit-rn';

const Icons: FC = () => {
  return (
    <Box>
      <Icon name="icon_addbold" />
    </Box>
  );
};

export default Icons;
